console.log("Vue Ok", Vue);

Vue.config.devtools = true;

const root = new Vue({
    el: "#root",
    data: {
        const user = {
            name: 'Nome Utente',
            avatar: '_io',
        };
        contacts,
    },
    methods: {},
})